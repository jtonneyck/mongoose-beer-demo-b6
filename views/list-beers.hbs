{{#each beers}}
    <h1>{{name}}</h1>
    <h3>{{tagline}}</h3>
    <h3>{{first_brewed}}</h3>
    <p>{{description}}</p>
    <a href="/beer/{{_id}}">detail view</a>
    <a href="/beer/delete/{{_id}}">Yuk, this beer is disgusting</a>
    <img src="{{image_url}}" alt="">
{{/each}}